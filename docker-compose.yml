version: "3.9"
   
services:
  queue:
    image: eclipse-mosquitto
    volumes:
      - ./mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
  api:
    build: si
    ports:
      - "80:80"
    depends_on:
      - queue
  back:
    build: SI_API
    ports:
      - "81:80"
    depends_on:
      - queue
  gui:
    build: gui
    ports:
      - "82:80"
    depends_on:
      - back
  gen:
    build: Generator
    depends_on:
      - api
